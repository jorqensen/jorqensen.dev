---
interface Props {
    href: string;
    brand?: "app" | "laravel" | "rust" | "c#" | "golang";
    target?: "_blank" | "_self" | "_parent" | "_top" | "framename";
}

const { href, brand = "app", target = "_self" } = Astro.props;
---

<a
    href={href}
    target={target}
    class="text-app-700 hover:text-app-600 transition-colors"
    ><slot /></a
>
<!-- underline decoration-app-600 decoration-2 underline-offset-4 transition-colors -->
