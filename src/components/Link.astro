---
interface Props {
    href: string;
    brand?: "app" | "laravel" | "rust" | "c#" | "golang";
    target?: "_blank" | "_self" | "_parent" | "_top" | "framename";
}

const { href, brand = "app", target = "_self" } = Astro.props;
---

<a
    href={href}
    target={target}
    class="underline decoration-app decoration-2 underline-offset-4 transition-colors"
    ><slot /></a
>
